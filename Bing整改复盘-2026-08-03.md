# Bing 整改复盘 - 2026-08-03

## 当前截图信号

- Bing Webmaster Tools 已验证 `https://soloclientlab.com/`。
- 首页报告显示最近周期约 `17` 次展示、`0` 次点击。
- Top Recommendation 提示：高质量域名的入站链接不足，可能影响在线可见性。
- Bing 邮件提示下一步应提交 sitemap 和重点 URL。

## 线上技术检查

- `https://soloclientlab.com/` 会 308 跳转到 `https://www.soloclientlab.com/`。
- `robots.txt` 可访问，并声明 sitemap：`https://www.soloclientlab.com/sitemap.xml`。
- `sitemap.xml` 当前包含 17 个公开 URL，包含首页、Research、Products、LeadRadar、NeedRadar 和 8 篇 Research。
- sitemap 中的主要 URL 大部分返回 200；`/products/leadradar` 单次检查曾超时/断开，复测 0.8 秒返回 200，先按偶发边缘响应观察。
- 首页 title、description、canonical、Organization/WebSite schema 正常。

## 判断

Bing 当前不是完全未发现站点，而是新站低权重阶段：

1. 技术可抓取基础已经具备。
2. Bing 只给了少量展示，说明页面进入初步测试，但排名和点击吸引力不足。
3. 最大短板不是页面数量，而是外部可信信号、URL 提交完整性、以及已有曝光页的 SERP 表达。
4. 站点规范域是 `www.soloclientlab.com`，Bing 里应同时添加并查看 `www` 属性，避免只盯裸域数据。

## 已完成整改

- 修复 `scripts/submit-indexnow.mjs`：产品页变更现在会被 `--changed` 推断并提交到 IndexNow。
- 补齐 fallback public paths：当 sitemap 获取失败时，IndexNow 仍会提交 `/products`、`/products/leadradar`、`/products/needradar-workflow-lab`。
- 给 sitemap 读取和 IndexNow POST 增加 10 秒超时，避免网络抖动时提交流程长期卡住。
- 已运行 `npm run indexnow:all`，Bing IndexNow 返回 `200 OK`，成功提交 sitemap 中的 17 个 URL。

## 立即执行清单

1. 在 Bing Webmaster Tools 添加并验证 `https://www.soloclientlab.com/` 属性。
2. 在 `www` 属性下提交 `https://www.soloclientlab.com/sitemap.xml`。
3. 在 URL Submission 手动提交这 6 个优先 URL：
   - `https://www.soloclientlab.com/`
   - `https://www.soloclientlab.com/research`
   - `https://www.soloclientlab.com/products`
   - `https://www.soloclientlab.com/products/leadradar`
   - `https://www.soloclientlab.com/tools/leadradar`
   - `https://www.soloclientlab.com/research/how-to-get-first-clients-without-network`
4. 部署后运行：

```bash
npm run indexnow:all
```

5. 在 Bing 的 URL Inspection 里检查 `/products/leadradar` 和首页，确认状态是可抓取、已发现、无 robots 阻挡。

## 一周内内容动作

优先围绕已有曝光页改，不急着新增相近页面。

1. 从 Bing `搜索性能 -> 查看完整报告` 导出最近 28 天：
   - Top queries
   - Top pages
   - 有展示但 0 点击的页面
2. 只优化连续出现或业务强相关的 query。
3. 对有展示但 0 点击页面，优先改：
   - title 是否包含人群和具体问题
   - meta description 是否说明读者能得到什么
   - 首屏 H1 是否和 query 语义一致
   - 正文是否有自然内链到 `/products/leadradar` 或相关 Research
4. 给 LeadRadar 获取 3-5 个真实外部入口：
   - Microsoft Edge Add-ons listing 上线后链接回产品页
   - GitHub/README 或产品介绍页链接
   - 一篇制造业/CNC 场景文章链接到 `/products/leadradar`
   - LinkedIn/X/论坛里的案例帖链接到对应 Research

## 暂时不要做

- 不要因为 17 次展示就大改全站定位。
- 不要删除已有 Research URL。
- 不要把所有页面 title 都塞进同一个大词。
- 不要只提交裸域 `soloclientlab.com`，后续以 `www.soloclientlab.com` 为主。

## 下次复盘口径

记录日期、数据窗口、Bing 属性、clicks、impressions、CTR、top queries、top pages，并把动作分成：

- 技术提交
- 页面标题/描述
- 内链
- 外链/分发
- 暂不处理
