# 后台页面 - 前端页面 - 动态静态对照表

基于当前项目代码整理，供日常对照使用。

更新时间：2026-07-29

V2 补充：

Research 文章发布不再保留后台 CTA、lead magnet、waitlist、related demand 关联的默认配置流程。文章的主要目标是 SEO 收录、GSC 表现和产品逻辑解释；产品推广优先通过 Markdown 正文内链连接到 Products、具体产品页、Research 栏目和相关文章。`cta_type` / `cta_target` 仅作为 legacy optional 数据保留，`cover image` 为可选社交分享资产。

新版后台主侧边栏只保留：

- `/admin`
- `/admin/metrics`
- `/admin/post-analytics`
- `/admin/posts`
- `/admin/subscribers`（Contacts）
- `/admin/waitlists`（Access Leads）
- `/admin/feedback`

旧的 `/admin/demands`、`/admin/resources` 保留直达兼容，不进入新版主导航。`/admin/subscribers`、`/admin/waitlists` 保留底层 URL 与数据，但在新版后台分别以 Contacts、Access Leads 的产品化语境出现。

## 总表

| 后台页面 | 对应前端页面 | 现在是动态还是写死 | 备注 |
|---|---|---|---|
| `/admin/posts` | `/research` 列表页 | 动态 | 文章列表来自数据库 |
| `/admin/posts/[id]` | `/research/[slug]` 详情页 | 动态 | 标题、摘要、正文、可选封面图、SEO 来自数据库；新文章优先用正文内链，legacy CTA 不触发自动文章模块 |
| `/admin/resources` | `/resources/client-acquisition-report` | 半动态 | Legacy 直达兼容，不进新版主导航 |
| `/admin/resources/[id]` | `/resources/client-acquisition-report/delivery`、`/resources/[slug]/download` | 动态 | Legacy 直达兼容，不进新版主导航 |
| `/admin/demands` | 不直接对应单独前端页面 | 动态数据 | Legacy 直达兼容，不进新版主导航 |
| `/admin/demands/[id]` | `/research/[slug]` 内部旧证据区 | 半动态 | 旧文章可继续显示已有 evidence，新文章不要求后台 demand 关联 |
| `/admin/subscribers` | 历史订阅 / 联系记录 | 动态采集 | 后台显示为 Contacts，保留原 URL 和数据 |
| `/admin/waitlists` | `/waitlist/[slug]` | 半动态 | 后台显示为 Access Leads，保留原 URL 和数据 |
| `/admin/metrics` | 无直接前端对应 | 动态 | 产品验证漏斗：Research -> Product page -> Trial -> Install -> Configure -> Review/export -> Feedback -> Paid pilot |
| `/admin/post-analytics` | `/research/[slug]` 文章表现 | 动态 | 文章浏览为辅助信号；V2 优先结合 GSC 看收录、曝光、点击和产品页内链访问 |
| `/admin` | 无直接前端对应 | 动态 | 后台总览看板，读数据库 |
| `/admin/login` | 无直接前端对应 | 动态 | 已切到 Supabase Auth 登录链路 |

## 现在如何在后台看文章数据

进入后台后，左侧导航点击 `Post Analytics`，路径是 `/admin/post-analytics`。

这里现在能看两层数据：

1. 顶部汇总卡片
- `Published posts tracked`
- `Total article views`

2. 每篇文章明细表
- `Post`：文章标题，可点回后台编辑页
- `Status`
- `Views`
- `Last activity`

当前这套数据主要对应文章页 `/research/[slug]` 的站内行为验证：

- `Views`：用户打开文章页时记录
旧 CTA / Subscribers 归因数据可以继续存在于数据库里，但不再进入新版 Post Analytics 默认视图。

V2 新文章更应该结合 GSC 复盘：

- 是否被收录
- 有哪些 query 曝光
- CTR 和平均排名如何
- 哪些文章通过正文内链带来产品页访问

补充说明：

- 如果刚上线还没有访问，`Coverage` 会显示 `Waiting`
- 需要先部署最新代码，并确保数据库里已经有 `post_events` 表，后台才会开始出现数据

## 前端主页面补充

| 前端页面 | 现在情况 | 备注 |
|---|---|---|
| `/` 首页 | 半动态 | 精选文章区是动态；Hero、proof、stats、底部转化区文案基本写死 |
| `/research` | 动态 | 文章列表动态；Hero 文案和 topic 按钮文案写死 |
| `/research/[slug]` | 动态 | 核心内容已后台化 |
| `/products` | 写死 | 产品索引页；当前承接正式 LeadRadar 产品 |
| `/products/leadradar` | 写死 + 动态事件 | 正式产品页；采集产品页查看、试用访问、安装入口等漏斗事件 |
| `/tools/leadradar` | 动态 Demo | 保留为体验桥接页，链接到正式产品页 |
| `/resources/client-acquisition-report` | 半动态 | 资源身份和交付方式动态；页面主文案大部分写死 |
| `/resources/client-acquisition-report/delivery` | 半动态 | 资源记录动态；说明文案还有固定文案 |
| `/newsletter` | legacy / redirect | 当前代码已重定向到 `/research`，不再作为主入口 |
| `/about` | 写死 | 整页内容来自 `lib/content.ts` |
| `/waitlist/[slug]` | 写死 + 动态采集 | 页面内容写死，但报名数据会进数据库 |

## 现在最值得继续后台化的部分

1. Products 产品索引页和产品详情页
2. LeadRadar 产品访问 / 试用申请后台
3. Research 栏目与 Products 的内链承接
